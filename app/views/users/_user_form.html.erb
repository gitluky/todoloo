<%= form_for user do |f| %>
  Email: <%= f.email_field :email %><br />
  Username: <%= f.text_field :name %><br />
  Password: <%= f.password_field :password %><br />
  Confirm Password: <%= f.password_field :password_confirmation %><br />
  <%= image_tag url_for(user.avatar), class: 'avatar-large' if user.avatar.attached? %><br />
  <% if user.provider.nil? %>
    Image: <%= f.file_field :avatar, class: 'avatar-large' %><br />
  <% end %>
  <%= f.submit button_name %>
<% end %>
