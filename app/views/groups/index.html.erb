<h2>My Groups</h2>
<%= link_to "Create Group", new_group_path %>
<div>
  <% @groups.each do |group| %>
    <div>
      <%= link_to group.name, group_path(group) %>
    </div>
  <% end %>
<div>
