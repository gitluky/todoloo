<%= form_for([@group, @task]) do |f| %>
  Name: <%= f.text_field :name %>
  Description: <%= f.text_area :description%>
  Assign to (optional): <%= f.collection_select(:assigned_to_id, User.member_of_group(@group), :id, :email ) %>
  <%=f.submit 'Create Task' %>
<% end %>
